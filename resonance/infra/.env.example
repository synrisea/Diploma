# Copy this file to .env and fill in real values. .env is gitignored;
# this .example file is committed so the required keys are documented.
# Docker Compose auto-loads .env from this same directory.

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
AWS_AVATARS_BUCKET_NAME=

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
