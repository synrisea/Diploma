{
  "name": "synthetic-avatars",
  "private": true,
  "version": "1.0.0",
  "description": "One-off tool: generates deterministic placeholder avatars (DiceBear, local, no scraping) for the fake/seeded user pool behind Resonance's synthetic and Google-scraped comments, and uploads them via the same S3 path identity-service's real avatar uploads use.",
  "scripts": {
    "generate": "node generate.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@dicebear/collection": "^9.2.2",
    "@dicebear/core": "^9.2.2",
    "dotenv": "^16.4.5",
    "pg": "^8.23.0",
    "sharp": "^0.33.5"
  }
}
